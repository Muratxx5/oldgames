import os
import ctypes

# DLL yolunu belirt
dll_path = r"C:\Users\kalip33\Documents\pydoc\hidapi-win\x64\hidapi.dll"

# DLL dosyasını elle yükle
ctypes.CDLL(dll_path)

# Artık hidapi çalışabilir
import hid

for d in hid.enumerate():
    print(f"VID: {hex(d['vendor_id'])}, PID: {hex(d['product_id'])}, Product: {d['product_string']}")
